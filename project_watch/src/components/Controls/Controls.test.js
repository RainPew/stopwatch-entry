
jest.useFakeTimers()
test("sau 1 giây thì func chạy",() => { 
    // const {handlePlayButton} = require('./Controls.tsx');

    // expect(handlePlayButton).toHaveBeenCalledTimes(1);
    // expect(handlePlayButton).toHaveBeenLastCalledWith(expect.any(Function), 1000);
 })